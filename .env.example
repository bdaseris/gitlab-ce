GITLAB_HOME=/data/gitlab

# SMTP (valeurs non sensibles — le mot de passe est géré via Docker secret)
SMTP_ADDRESS=mta.example.com
SMTP_PORT=587
SMTP_USER_NAME="GitLab Notifier <gitlab-info@example.com>"
SMTP_DOMAIN=example.com
